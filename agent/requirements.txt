# AgriTrust Underwriting Agent — Python dependencies
# Core async HTTP + data modeling
httpx>=0.27.0
pydantic>=2.6.0

# Real off-chain data ingestion (no API key required)
open-meteo>=0.3.0      # weather API

# Optional LLM underwriting (set OPENAI_API_KEY / OPENAI_BASE_URL to enable)
openai>=1.30.0

# CLI + structured logging
rich>=13.7.0
typer>=0.12.0

# Casper key handling + x402 EIP-712 signing helpers
cryptography>=42.0.0
